-- NOTE: select block of code and then in VISUAL mode type `Rayso`, proceed to browser
return {
  "TobinPalmer/rayso.nvim",
  opts = {
    options = {
      theme = "breeze",
      background = true,
      dark_mode = true,
      title = "Untitled",
      padding = 32,
      logging_enabled = false,
    },
  },
  event = "VeryLazy",
}
